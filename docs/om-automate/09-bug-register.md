# Bug and Risk Register

## Method

This register is based on code tracing, static searches, focused tests, a native startup, and an authenticated browser smoke test at upstream commit `9844a2f9a1996b8c8135a9e7bbde6a72f41df5ed`.

SAFE-002 checkpoint-B entries were reconciled against the current working tree. Its latest focused gate is **77 passed, 1 warning**. The final-state restricted full suite reached **4,558 passed, 3 skipped, 20 failed, 8 warnings in 132.80 seconds**; all 20 failures are sandbox-denied loopback TCP, Unix-socket, or DNS cases and no registry/policy assertion failed. A socket-enabled rerun could not start because escalation hit the account usage limit until **2026-07-25**, so checkpoint B does not have a passing full-suite result. Checkpoint A's older socket-enabled 4,538-pass result applies only to its prior code.

The initial broad scan found approximately 256 `TODO`, 2 `FIXME`, 5 `HACK`, 14 `XXX`, 16 `NotImplemented`, 54 `temporary`, 25 `deprecated`, 1,747 `except Exception`, 100 `console.log`, 474 `localhost`, and 2,084 `Odysseus` matches outside the new audit documents and virtual environment. These counts include legitimate documentation/tests and are triage inputs, not automatically bugs.

Severity definitions:

- **Critical** — credible secret compromise, privilege takeover, irreversible action, or silent data loss.
- **High** — broken safety boundary, major correctness/reliability failure, or release blocker.
- **Medium** — material degradation or maintainability risk with a bounded workaround.
- **Low** — polish, portability, or documentation defect with limited immediate impact.

## Open issues

| ID | Severity | User impact | Reproduction steps | Root cause | Proposed fix | Test requirement | Status |
|---|---|---|---|---|---|---|---|
| OM-BUG-001 | Critical | An agent can execute arbitrary shell/Python with application secrets in its environment. | Enable Agent mode, expose shell/Python tools, and request a command that reads environment variables or sensitive files. | `src/tool_execution.py:519-540`, `src/agent_tools/subprocess_tools.py:103-153`, and `routes/chat_routes.py:873-881` lack a secret-stripped sandbox and generic approval boundary; actual timeout can be one hour. | Run dangerous tools in a constrained worker with an explicit workspace, allowlist, scrubbed environment, resource limits, short policy-owned timeout, and approval. | Adversarial prompt/tool tests must prove secrets are absent, denied commands never start, timeout kills descendants, and every attempt is audited. | Confirmed; open |
| OM-BUG-002 | Critical | File tools can read or alter authentication, settings, OAuth, vault, key, database, and MCP state. | Ask an agent to read a known file below the configured data directory. | `src/tool_execution.py:61-151` treats the broad data directory as an allowed root; the sensitive-path denylist does not cover all privileged state. | Give each tool narrow purpose-specific roots/capabilities; deny application control-plane data regardless of administrator role; isolate user documents from secrets. | Symlink, alias, case, macOS `/private` realpath, traversal, race, read/write/delete, and sensitive-file matrix tests. | Confirmed; open |
| OM-BUG-003 | Critical | A corrupt/unreadable auth store can make the app appear unconfigured, allowing an unauthenticated user to claim the first administrator account. | Corrupt or make `data/auth.json` unreadable, restart, then call first-run setup. | `core/auth.py:121-141` falls back to `{}`; setup routes interpret missing configuration as a valid first-run state. Docker setup failure is also ignored. | Fail closed into a recovery-only state; distinguish absent-on-first-boot from corrupt/unreadable; require a local recovery secret/console operation; make setup failure fatal. | Corrupt, truncated, wrong-owner, wrong-mode, concurrent-write, and Docker setup-failure tests must never expose public admin creation. | Confirmed; open |
| OM-BUG-004 | Critical | Pressing Stop may end visible streaming while a side-effecting tool keeps running. | Start a slow effectful tool in Agent mode, press Stop, and observe the tool/process/provider afterward. | `src/agent_loop.py` created a child tool task without `finally` cancellation; `src/agent_runs.py` cancelled only the outer drain/generator. | Propagate structured cancellation to the tool child and adapter, wait for termination, persist `cancelled`, and verify whether any effect occurred. | Deterministic in-flight tool cancellation tests, subprocess descendant kill tests, and provider-side partial-effect reconciliation tests. | **Owned in-process child fixed in SAFE-001.** Deterministic unit/browser shell probe passed; durable remote-effect reconciliation remains SAFE-005/007. |
| OM-BUG-005 | Critical | Free-form model prose can become an executable action, increasing prompt-injection risk. | Use a non-native tool-call model that emits a fenced, XML, DSML, StepFun, Gemma, or raw JSON tool block. | `src/tool_parsing.py:1235-1407` and `src/agent_loop.py:2175-2223` accept many structured-text syntaxes directly from model output. | Require a typed action envelope from a capability-tested structured-output channel; treat all ordinary content and retrieved text as non-executable. | Injection corpus across email/web/RAG/files plus malformed/ambiguous/duplicate envelope tests; assert zero tool starts. | Confirmed; open |
| OM-BUG-006 | High | Destructive or consequential tools lack consistent approvals, scopes, reversibility metadata, and verification. | Ask for an email send, file deletion, shell mutation, calendar change, or MCP write and compare enforcement paths. | Policy is a per-turn denylist plus coarse admin/plan checks; email has a special isolated staging path; no generic approval/action record exists. | Introduce one canonical registry and policy engine with permissions, risk, confirmation, reversibility, timeout/retry/idempotency, audit, and readback contracts. | Registry completeness tests and deny-by-default tests for every tool; UI/API approval lifecycle and expiry/concurrency tests. | Confirmed; open |
| OM-BUG-007 | High | A restart can lose agent run state and a retry can duplicate an external effect. | Start or complete an external effect, terminate before final persistence, restart, then retry. | `src/agent_runs.py` stores active runs/events in a process-local `_RUNS`; there is no durable action cursor, idempotency ledger, or compensation record. | Persist run/action/approval/attempt/result/verification state before execution; require idempotency keys and reconciliation on restart. | Crash at every state transition; restart/replay; duplicate webhook/provider callback; exactly-once or explicitly reconciled outcome tests. | Confirmed; open |
| OM-BUG-008 | High | “Incognito” messages are briefly written to SQLite despite the UI/service claiming memory-only operation. | Create an incognito message and inspect `chat_messages` before cleanup/restart. | `routes/chat_helpers.py:431-438,1010-1078` calls `sess.add_message`; `core/models.py:94-107` persists through `SessionManager`, then later cleanup deletes it. | Use a genuinely non-persistent session/message implementation or rename the feature and disclose transient persistence. | Assert no database row, vector, log, backup, task, or crash residue for incognito content. | Confirmed; open |
| OM-BUG-009 | High | Typed metadata can imply controls that the executor does not enforce, and cookbook diagnostics can expose another administrator's task output. | Compare a checkpoint-B definition with `execute_tool_block()`, then call `tail_serve_output` as an administrator with an unrelated syntactically valid session ID. | SAFE-002 repaired the original tag/import defect and added an immutable 77-tool metadata registry, but legacy prompt/index/UI/dispatcher/MCP views remain independent. `tail_serve_output` has a non-admin gate but no caller ownership or fresh-launch sequence check. | Route execution and generated views through the registry/policy boundary; validate diagnostic session ownership and the `serve_model` → `list_served_models` launch sequence. | Every registered tool round-trips schema → validation → policy → adapter; runtime decisions match metadata; cross-owner and stale-session diagnostics deny. | **Partially fixed in SAFE-002 A/B:** reachability, cold imports, immutable inventory, projections, and non-admin gate are covered; runtime enforcement and diagnostic ownership/sequence remain open. |
| OM-BUG-010 | High | A clean import of the tool schema module failed depending on import order. | In a fresh interpreter import `src.tool_schema_catalog`, `src.tool_schemas`, and `src.tool_registry` independently. | The baseline circular initialization was removed by moving neutral types/identity and handler-free schemas below the compatibility facade. Broader application imports still need randomized architecture coverage. | Keep the facade thin and dependency-neutral; add a complete independent/randomized production-module import gate before resolving the systemic import risk. | Cold-import every production module independently and in randomized orders. | **Confirmed cold-import defect fixed in SAFE-002 A/B;** broader randomized module/layer gate remains open. |
| OM-BUG-011 | High | Raw hidden model reasoning is exposed in the chat UI. | Chat with `qwen3:1.7b`; expand `View thinking process`. | The stream/parser/renderer preserves and displays model thinking content rather than emitting a bounded action/status summary. | Strip private reasoning at the provider boundary; store/display only user-safe rationale, tool state, citations, and verified outcomes. | Browser and JS tests with multiple reasoning syntaxes; assert sensitive trace text is absent from DOM, API payloads, persistence, and logs. | Confirmed; open |
| OM-BUG-012 | High | Incoming scheduled-task webhook URLs are replayable bearer secrets without modern request authenticity controls. | Capture a valid webhook URL and replay/duplicate requests or send an oversized body. | The inbound task webhook lacks HMAC signature, signed timestamp, nonce/replay cache, idempotency key, strong body/rate controls, and lifecycle rotation. | Add versioned HMAC signing, constant-time verification, timestamp window, nonce/idempotency storage, body/rate limits, rotation/revocation, and audit. | Forged, stale, replayed, oversized, concurrent duplicate, rotated-key, and canonicalization tests. | Confirmed; open |
| OM-BUG-013 | High | OAuth flows and scopes are not uniformly hardened. | Initiate Google OAuth, retain/replay state, inspect requested scopes and disconnect behavior; compare legacy MCP OAuth. | Google state lacks expiry/one-time consumption/PKCE; mail scope is broad; revoke is incomplete; legacy MCP state can be predictable and stored plainly. | PKCE, short-lived one-time state bound to user/session/provider, least scopes, revoke/disconnect, encrypted token metadata, and reauth handling. | State replay/cross-user/expiry, PKCE, scope snapshot, token refresh/rotation/revocation, and log-redaction tests. | Confirmed; open |
| OM-BUG-014 | High | Authentication, database, settings, session, environment, and log files are created world-readable on the local host. | Run fresh native setup and inspect file modes. | Atomic writers and setup paths do not consistently set `0600`; containing directories are not consistently private. | Central secure file/directory creation, startup permission repair with safe ownership checks, and platform-specific ACL documentation. | Fresh install, upgrade, backup/restore, concurrent atomic write, macOS/Linux mode tests; Windows ACL acceptance test. | Confirmed; open |
| OM-BUG-015 | High | Encryption keys and encrypted data are colocated, lack rotation, and are included together in unencrypted backups. | Inspect `data/.app_key` and backup contents. | Local encryption uses a single colocated key; backup archives capture key plus ciphertext; no key version/rotation or encrypted export format. | Introduce key-provider interface/key versions, rotation, passphrase or OS-keystore-backed encrypted backups, and explicit recovery procedure. | Restore with correct/wrong/missing keys, rotation across old/new rows, tamper detection, and no-plaintext backup tests. | Confirmed; open |
| OM-BUG-016 | High | The Python behaviour suite is not a release gate and is order/platform sensitive. | Inspect `.github/workflows/ci.yml:103-109`; run scheduler→plan order and macOS workspace confinement test. | Pytest is `continue-on-error`; tests leak `sys.modules` fakes and assume non-canonical macOS paths; socket tests depend on sandbox capability. | Repair isolation/portability, separate hermetic/unit/integration gates, and make required suites blocking. | Randomized-order, repeat, macOS/Linux/Windows, socket-enabled integration, and clean full-suite run. | Confirmed; open |
| OM-BUG-017 | High | Installation is not repeatable across time or supported platforms. | Resolve `requirements.txt` or pull container images on different dates; compare Python 3.11 CI, 3.13 launcher, and 3.14 Docker. | Most Python dependencies are unpinned; Chroma/ntfy/base images float; apt/download artifacts lack immutable locks/checksums. | Produce platform-tested lock files/SBOM, pin images/digests and artifacts, align runtimes, and add controlled update automation. | Offline/cache rebuild, checksum verification, fresh platform installs, dependency licence/vulnerability scan, and reproducibility comparison. | Confirmed; open |
| OM-BUG-018 | High | Database upgrades can partially apply and continue without a reliable schema version or rollback. | Inspect or interrupt import-time migrations in `core/database.py`; restart with a partially changed DB. | Dozens of ad-hoc `ALTER` routines run at import/startup, often catch broad exceptions, and lack a version ledger/transactional migration framework. | Introduce versioned migrations, preflight/backup, transactional or resumable steps, forward/backward compatibility, and rollback policy. | Upgrade from representative historical fixtures, interrupted migration recovery, downgrade/rollback, data checksum, and repeat-idempotency tests. | Confirmed; open |
| OM-BUG-019 | Medium/High | Readiness and Compose health do not represent usable system state. | Call `/api/ready` unauthenticated and inspect Compose dependencies/health checks. | Auth middleware protects readiness; it checks only DB/data writeability; app/Chroma/ntfy lack meaningful health checks and Chroma waits only for start. | Public non-sensitive liveness/readiness, dependency classification (required/degraded), Compose health checks, start periods, and dependency gates. | Unauthenticated probes, each dependency down/slow/corrupt, startup race, recovery, and no-secret response tests. | Confirmed; open |
| OM-BUG-020 | Medium/High | The configured Compose log bind does not receive the main application log. | Start with `APP_LOGS_DIR`; inspect host `logs/` versus `data/logs/app.log`. | Compose mounts `/app/logs`, while logging resolves beneath `DATA_DIR` at `/app/data/logs`. | Use one documented log path/volume or write structured logs to stdout with optional secure file sink. | Compose persistence/rotation/permissions, stdout capture, backup exclusion, and secret-redaction tests. | Confirmed; open |
| OM-BUG-021 | Medium/High | MCP subprocesses inherit the full app environment and configured MCP environment values are stored/returned too openly. | Configure a local MCP server that prints its environment; inspect MCP configuration API/storage. | `src/mcp_manager.py` launches children from the parent environment and persists extra environment values without a secret-reference boundary. | Minimal environment allowlist, per-server secret references resolved only in worker, masked APIs, encrypted storage, process isolation. | Malicious MCP echo test, API masking, at-rest inspection, rotation/revoke, and child-process boundary tests. | Confirmed; open |
| OM-BUG-022 | Medium/High | Session, TOTP, backup-code, and API bearer lifecycle controls are weaker than required for a privileged control system. | Inspect auth storage, cookie defaults, backup codes, session records, and central bearer middleware. | Long-lived plaintext sessions, weak/plain TOTP recovery material, opt-in `Secure`, and allow-by-default bearer routing lack expiry/rotation/event visibility. | Hash one-time recovery codes, encrypt TOTP seeds, rotate/revoke sessions/tokens, secure cookie defaults for network mode, allowlist token routes/scopes, and audit auth events. | Theft/replay/rotation/logout-all, clock skew, recovery reuse, cookie/proxy, token expiry/scope, and auth-event UI tests. | Confirmed; open |
| OM-BUG-023 | Medium | Bash/Python may run much longer than the model/user contract implies. | Compare the 60-second prompt, 60/30-second compatibility constants, 3,600-second registry records, and 3,600-second subprocess defaults; then run a sleeping tool. | Checkpoint B records timeout metadata but the executor does not consume it. Four independent sources still disagree, and the worker's one-hour value is effective. | Make the worker consume policy-owned registry deadlines; generate prompt/UI values from the same decision and reject caller overrides outside policy. | Per-tool boundary, override rejection, process-tree kill, progress, cancellation, and audit-duration tests must assert the enforced registry value. | **Metadata added in SAFE-002 B; runtime drift remains open.** |
| OM-BUG-024 | Medium | Shutdown produces MCP cancel-scope warnings and may not release resources cleanly. | Start then stop the native app with bundled MCP servers connected. | MCP client scopes are exited from a different task/lifecycle context. | Make connection ownership explicit and close each client in its owning task/task group before loop teardown. | Repeated start/stop, signal termination, partial startup, connection failure, and leaked-process/task tests. | Confirmed; open |
| OM-BUG-025 | Medium | Error handling is too broad and can hide partial failure. | Search for `except Exception` and exercise unavailable integrations/migrations. | Approximately 1,747 broad catches mix expected degradation with unexpected correctness/security failures; several log only debug or continue. | Define typed domain/integration errors, fail closed for security/data paths, structured error codes, and narrow exception handling. | Fault injection at persistence/provider/tool boundaries; assert correct user state, log severity, audit record, and rollback. | Confirmed systemic risk; triage open |
| OM-BUG-026 | Medium | Monolithic and inverted dependencies make changes fragile and produce real test/import drift. | Inspect large composition/route/frontend modules and dependency direction. | Business logic lives in routes, `core` imports `src`, `src/tools/cookbook.py` imports a route validator, and compatibility modules replace `sys.modules`. | Introduce domain/application/provider layers incrementally with compatibility facades and dependency rules. | Architecture import/layer tests, route contract tests, and characterization tests before extraction. | Confirmed; open |
| OM-BUG-027 | Medium | Application/API version and product/source metadata do not yet provide a trustworthy modified-source offer. | Inspect `/api/version`, UI about surfaces, Docker image contents, and `.dockerignore`. | Version only reports `1.0.2`; docs/Markdown including licence/source notices are excluded from the image; no exact modified commit/source link is exposed. | Add central build metadata with product/upstream/modified commit/licence/source URL and package required legal files in every artifact. | Build artifact inspection, offline About page, API metadata, source-link exactness, and legal-file presence tests. | Confirmed; open |
| OM-BUG-028 | Medium | Licence statements conflict, risking incorrect redistribution guidance. | Compare root `LICENSE`/README with `ACKNOWLEDGMENTS.md:141-159` and `Dockerfile:72-77`. | The repository is AGPL-3.0-or-later, while those locations describe core/MIT compatibility. | Correct inaccurate wording without removing upstream notices; generate third-party notices/SBOM; obtain legal review for release language. | Static licence consistency and artifact notice-presence checks. | Confirmed; open |
| OM-BUG-029 | Medium | Compatibility-sensitive Odysseus identifiers make a blind rebrand capable of breaking existing data/integrations. | Enumerate `ODYSSEUS_*`, cookies, headers, storage keys/events, PWA caches, Chroma collections, SQLite values, service names, paths, and tuned-model prefixes. | Branding and protocol/storage identifiers are intermingled across thousands of matches. | Centralize visible brand metadata; preserve or dual-read old identifiers; migrate writes deliberately with rollback and document each retained alias. | Existing-data upgrade, old/new env, cookie/session, localStorage, header, database/vector, service, and rollback tests. | Confirmed; open |
| OM-BUG-030 | Low/Medium | Platform-path normalization can cause false test failures and confusing confined-file output on macOS. | Run `tests/test_workspace_confine.py::test_glob_confined_e2e` where `/tmp` or `/var/folders` resolves through `/private`. | Test/output comparison mixed caller spelling with canonical realpaths and echoes rejected patterns. | Compare canonical discovered paths while separately testing safe user-pattern echo; later improve sanitized errors. | macOS alias, Linux symlink, Windows drive/case, and sanitized-error tests. | **Portability assertion fixed;** broader sanitized-error review remains open. |
| OM-BUG-031 | Medium | `update_plan` can report success without an active plan, then throw in the browser instead of updating the dock. | Execute `UpdatePlanTool` with any non-empty checklist, stream its `plan_update`, and inspect `static/js/chat.js` handling. | The backend does not receive/check active-plan state despite advertising a no-op without one; the frontend calls `_setStoredPlan`, which has no definition in the browser sources. | Bind updates to a versioned active plan/run record, reject or no-op when none exists, and implement one tested plan-store/dock update API. | Backend absent/stale/active-plan tests plus JS/DOM live and restored-plan tests; assert no undefined symbol and exact version update. | Confirmed; open |
| OM-BUG-032 | Medium/High | Listing or reading one chat's background jobs can kill timed-out jobs and prune records/files belonging to other chats. | Seed running/retained jobs in two sessions, then call `manage_bg_jobs` list or output in one session after a timeout/retention boundary. | `list_for_session()` and `get()` call global `bg_jobs.refresh()` before ownership filtering. `refresh()` reconciles all jobs, kills expired process trees, saves global status, and prunes all sessions. | Separate pure scoped observation from mutating reconciliation; perform reaping in the owned monitor/service and authorize each mutation before it occurs. | Two-session tests must prove list/output do not kill, persist, prune, or delete; reconciliation/kill require explicit owned paths and verified process state. | Confirmed; open |
| OM-BUG-033 | Medium | Native bash, Python, filesystem, and web calls first target removed built-in MCP server IDs, adding fragile error-dependent routing. | Start with the normal built-in MCP set and call each name in `_MCP_TOOL_MAP`; observe `mcp__bash__*`, `mcp__python__*`, `mcp__filesystem__*`, or web calls before native fallback. | Those servers were folded into in-process handlers and removed from `_BUILTIN_SERVERS`, but `_MCP_TOOL_MAP` still contains their routes. Fallback occurs only for a result whose error text contains `not connected`. | Route native tools directly through canonical bindings; reserve MCP dispatch for validated connected dynamic/built-in MCP identities and use typed failure states rather than string matching. | Live dispatch tests must prove each native tool makes zero MCP calls; connected dynamic-name collision and non-`not connected` error tests must fail closed. | Confirmed; open |
| OM-BUG-034 | Medium | The LLM can lose the reason a bash/Python tool timed out and see only exit code 124. | Format a timeout result containing `error`, empty `stdout`/`stderr`, and `exit_code=124` with `format_tool_result()`. | Presence of the `stdout` key selects the first formatter branch; that branch emits streams and exit code but never the `error` field, and the later error branch is unreachable. | Normalize typed tool results and render error/status consistently to the LLM, UI, SSE, persistence, and audit consumers. | Empty/non-empty stdout/stderr timeout matrices must retain the timeout error once, preserve exit code 124, and match browser-visible status. | Confirmed; open |

## Positive controls to retain

- Authentication is enabled by default; localhost auth bypass is disabled.
- Non-administrators are blocked from coarse high-risk capabilities.
- File confinement already uses realpath checks and includes useful symlink protections, although its allowed roots are too broad.
- External content is explicitly marked untrusted in prompt assembly.
- Recursive settings redaction, CSP/security headers, encrypted/masked Google values, bcrypt API-token hashing, outbound-webhook SSRF/DNS pinning/HMAC, and loopback-only Compose binds are valuable foundations.
- Provider fallback avoids switching after visible response content has begun, preventing mixed-provider answers.
- Repetition breaking and detached SSE replay provide useful resilience primitives, but need durable state and cancellation integration.

## Release gate

No issue rated Critical or High may remain unresolved for the production-quality local release. A status may move to `Resolved` only when the focused regression, related suite, full blocking suite, startup, and applicable manual browser/provider verification all pass and the fix is linked to an ADR or implementation record.
